import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

const container = document.getElementById('root');
const element = (
  <div className='label'>Hello React</div>
)
ReactDOM.render(element, container);
